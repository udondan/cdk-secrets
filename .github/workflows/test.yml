---
name: Build packages

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Where am I
        run: pwd

      - name: Check contents
        run: ls -al

      - uses: udondan/jsii-publish@master
        with:
          fetch-depth: 1

      - name: Check contents
        run: ls -al
#- name: Get the version
#  id: get_version
#  run: echo ::set-output name=VERSION::${GITHUB_REF/refs\/tags\//}
